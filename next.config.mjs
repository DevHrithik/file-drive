/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "accomplished-albatross-0.convex.cloud",
      },
    ],
  },
};

export default nextConfig;
